/**
 * a.k.a. Bad Gear
 */


export default class AudioPilz {

  static printBoxesGag(customGagMessage) {
    console.info(
      "At first glance, " +
      `this program file is ticking all the "${customGagMessage}" boxes! ✅`
    );
  }

}
